[project]
name = "demo-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai>=2.53.0",
    "python-dotenv>=1.2.2",
    "truststore>=0.10.4",
]

[tool.pyright]
venvPath = "."
venv = ".venv"
